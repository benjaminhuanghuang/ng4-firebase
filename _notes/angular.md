- Upgrade npm 
  npm install -g npm 

- Angular Cli
  https://cli.angular.io/
  ```
  npm i -g @angular/cli
  ng --version
  ng new my-app  
  or ng new appName --directory ./
  cd my-app
  ng serve
  ```